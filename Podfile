# Uncomment the next line to define a global platform for your project
 platform :ios, '9.0'

target 'Biology Review Quiz' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Biology Review Quiz
  pod 'Firebase'
  pod 'Firebase/Core'
  pod 'Firebase/Auth'
  pod 'Firebase/Database'
  pod 'Firebase/Storage'
  pod 'Firebase/Crash'
  pod 'Firebase/AdMob'
  pod 'FirebaseUI/Auth'
  pod 'SwiftKeychainWrapper'

  target 'Biology Review QuizTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'Biology Review QuizUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
